import { ArrowRight, CheckCircle2, FileText, Scale, UserRound } from "lucide-react";
const parts = [[UserRound,"CLAIM","Ramesh Kumar Sharma"],[FileText,"EVIDENCE","Identity Proof"],[Scale,"RULE","Name must match Jan Aadhaar family record"],[CheckCircle2,"RESULT","Match ✓"]] as const;
export function EvidenceTrail() { return <section className="section-rule"><div className="section-shell py-20 lg:py-28"><div className="max-w-2xl"><p className="utility text-xs text-[var(--accent)]">04 / EVIDENCE TRAIL</p><h2 className="display mt-4 text-3xl sm:text-4xl">Every result is explainable — never a black box.</h2></div><div className="mt-10 flex flex-col gap-3 md:flex-row md:items-stretch">{parts.map(([Icon,label,text],index)=><div className="contents" key={label}><div className="flex min-h-32 flex-1 flex-col border border-[var(--line)] bg-[var(--bg-elevated)] p-5"><div className="flex items-center gap-2 utility text-[10px] text-[var(--accent)]"><Icon size={14}/>{label}</div><p className={`mt-auto pt-5 text-sm leading-6 ${label === "RESULT" ? "text-[var(--pass)]" : "text-[var(--ink)]"}`}>{text}</p></div>{index < parts.length - 1 && <ArrowRight className="mx-auto shrink-0 self-center text-[var(--accent-dim)] max-md:rotate-90" size={18}/>}</div>)}</div></div></section>; }
