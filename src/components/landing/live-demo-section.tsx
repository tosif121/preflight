"use client";
import { CheckCircle2, CircleX, AlertTriangle } from "lucide-react";
import { useState } from "react";
export function LiveDemoSection() { const [resolved,setResolved] = useState(false); const passed = resolved ? 13 : 12; return <section className="section-rule" id="demo"><div className="section-shell py-20 lg:py-28"><div className="max-w-2xl"><p className="utility text-xs text-[var(--accent)]">02 / LIVE PREFLIGHT DEMO</p><h2 className="display mt-4 text-3xl sm:text-4xl">The checks are clear enough to act on.</h2></div><div className="mt-10 grid gap-8 border border-[var(--line)] bg-[var(--bg-elevated)] p-5 sm:p-7 lg:grid-cols-[220px_1fr]"><div className="flex flex-col items-center justify-center border-b border-[var(--line)] pb-7 lg:border-b-0 lg:border-r lg:pb-0 lg:pr-7"><div className="grid size-32 place-items-center rounded-full border-8 border-[var(--pass)]"><div className="text-center"><p className="utility text-xl text-[var(--ink)]">{passed}/14</p><p className="mt-1 text-xs text-[var(--ink-muted)]">checks passed</p></div></div><p className="mt-5 utility text-[10px] text-[var(--ink-muted)]">RAJASTHAN / FAMILY-INCOME</p></div><div className="space-y-5"><CheckGroup label="BLOCKERS" color="var(--blocker)" icon={<CircleX size={16}/>}><Check title="name_consistency" note={resolved ? "Identity evidence now matches the family record." : "Name differs from Jan Aadhaar family record."} status={resolved ? "passed" : "blocker"} action={!resolved ? () => setResolved(true) : undefined} /></CheckGroup><CheckGroup label="WARNINGS" color="var(--warn)" icon={<AlertTriangle size={16}/>}><Check title="certificate_use_by_date" note="Review the intended use date before submission." status="warning" /></CheckGroup><CheckGroup label="PASSED" color="var(--pass)" icon={<CheckCircle2 size={16}/>}><Check title="income_coverage" note="Income proof provided for each earning member." status="passed" /><Check title="address_consistency" note="Address is consistent across submitted documents." status="passed" /><Check title="document_quality" note="All scans meet the readable-document threshold." status="passed" /></CheckGroup></div></div></div></section>; }
function CheckGroup({label,color,icon,children}:{label:string;color:string;icon:React.ReactNode;children:React.ReactNode}) { return <div><p className="utility mb-2 flex items-center gap-2 text-[10px]" style={{color}}>{icon}{label}</p><div className="divide-y divide-[var(--line)] border-y border-[var(--line)]">{children}</div></div>; }
function Check({title,note,status,action}:{title:string;note:string;status:string;action?:()=>void}) { const color=status === "passed" ? "var(--pass)" : status === "warning" ? "var(--warn)" : "var(--blocker)"; return <div className="flex items-center gap-4 py-3"><span className="size-2 rounded-full" style={{background:color}}/><div className="min-w-0 flex-1"><p className="utility text-xs">{title}</p><p className="mt-1 text-xs text-[var(--ink-muted)]">{note}</p></div>{action && <button onClick={action} className="rounded-lg bg-[var(--blocker)] px-3 py-1.5 text-xs text-white shadow-sm hover:opacity-90">Resolve</button>}</div>; }
